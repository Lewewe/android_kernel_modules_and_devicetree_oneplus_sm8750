If you need to fork, this repo requires you to fork it into the kernel folder.

Fork command: ```git clone https://github.com/Lewewe/android_kernel_modules_and_devicetree_oneplus_sm8750 -b [branch you need] kernel/```
